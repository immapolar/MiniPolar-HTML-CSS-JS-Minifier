<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= heading %></h1>
    <p>This is a simple EJS page.</p>
    
    <% if (showList) { %>
        <ul>
            <% items.forEach(item => { %>
                <li><%= item %></li>
            <% }); %>
        </ul>
    <% } %>
    
    <%- include('partials/footer') %>
</body>
</html>
